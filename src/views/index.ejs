<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Matcher - Find Your Perfect Job</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #e5e5e5;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background-color: #e5e5e5;
            width: 100%;
            max-width: 800px;
            min-height: 600px;
            border: 3px solid #333;
            display: grid;
            grid-template-columns: 1fr 1fr;
            position: relative;
        }

        .left-section {
            background-color: #e5e5e5;
            padding: 40px 30px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .right-section {
            background-color: #d0d0d0;
            padding: 40px 30px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            border-left: 3px solid #333;
        }

        .header {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            text-align: center;
            padding: 30px;
            background-color: #e5e5e5;
            border-bottom: 3px solid #333;
        }

        .header h1 {
            font-size: 3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 1.1rem;
            color: #555;
            margin: 0;
        }

        .content-area {
            margin-top: 140px;
        }

        .section {
            margin-bottom: 40px;
        }

        .section h2 {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            text-align: center;
        }

        .section p {
            font-size: 0.95rem;
            line-height: 1.5;
            color: #444;
            text-align: justify;
        }

        .join-section h2 {
            font-size: 2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 30px;
            text-align:center;
        }

        .buttons {
            display: flex;
            gap: 20px;
        }

        .btn {
            background-color: #666;
            color: white;
            border: none;
            padding: 15px 25px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            border-radius: 0;
            transition: all 0.3s ease;
            min-width: 100px;
        }

        .btn:hover {
            background-color: #555;
            transform: translateY(-2px);
        }

        .btn:active {
            transform: translateY(0);
        }

        @media (max-width: 768px) {
            .container {
                grid-template-columns: 1fr;
                grid-template-rows: auto 1fr 1fr;
            }

            .right-section {
                border-left: none;
                border-top: 3px solid #333;
            }

            .header h1 {
                font-size: 2.5rem;
            }

            .content-area {
                margin-top: 120px;
            }

            .buttons {
                flex-direction: column;
                width: 100%;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Matcher</h1>
            <p>Find jobs that match your skill set & Applicants that match your job</p>
        </div>

        <div class="left-section">
            <div class="content-area">
                <div class="section">
                    <h2>What sets us apart?</h2>
                    <p>Often times Job boards just let applicants apply, and let you do some filtering using very basic filters. With no weighting, or fine tuning. However we fine tune the results to give you applicants which meet as many of your requirements as possible. In addition we will setup the interview, and tests for you</p>
                </div>

                <div class="section">
                    <h2>Methods</h2>
                    <p>Using advanced AI techniques we're able to effectively evaluate a person's portfolio, and evaluate their skills. Then we're able to match the jobs in the highest demand of those skills.</p>
                </div>
            </div>
        </div>

        <div class="right-section">
            <div class="join-section">
                <h2>Join today!</h2>
                <div class="buttons">
                    <button class="btn" onclick="handleEmployerClick()">Employers</button>
                    <button class="btn" onclick="handleApplicantClick()">Applicants</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        function handleEmployerClick()
        {
            // Example target URL with GET parameters
            const targetUrl = '/register?role=applicant';
            
            // Redirect the user to the URL
            window.location.href = targetUrl;        }

        function handleApplicantClick() {
            // Example target URL with GET parameters
            const targetUrl = '/register?role=applicant';
            
            // Redirect the user to the URL
            window.location.href = targetUrl;
        }


        // Add some subtle interactivity
        document.addEventListener('DOMContentLoaded', function() {
            const buttons = document.querySelectorAll('.btn');
            buttons.forEach(button => {
                button.addEventListener('mouseenter', function() {
                    this.style.boxShadow = '0 4px 8px rgba(0,0,0,0.2)';
                });
                button.addEventListener('mouseleave', function() {
                    this.style.boxShadow = 'none';
                });
            });
        });
    </script>
</body>
</html>
